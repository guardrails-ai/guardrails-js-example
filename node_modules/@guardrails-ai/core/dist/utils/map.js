export async function map(pyObjects = [], callback) {
    const results = [];
    for await (const pyObject of pyObjects) {
        const response = await callback(pyObject);
        results.push(response);
    }
    return results;
}
